`Golang

## Golang : The language itself was written in GO.

**INTRODUCTION**
-------------------
* Go version V1.25 (Aug/2025), latest Go V1.26 (Feb,2026).
* For learn Golang Boot.dev's
* The program is saved with .go extension and it is a UTF-8 text file.
* Go language is a STATICALLY typed COMPILER based programming language.
* It is the COMBINATION of C++ and Python. Python is a simple Syntax and C++ is a faster language.
* GO tool can RUN directly, Without VIRTUAL MACHINE.(like JVM)
* Go developed by GOOGLE in 2007 (Robert Giesemer, Rob Pike, Ken thompson)and made OPEANSOURCE with GO V1.0 in 2009.
* Executables are different for OS. (If you developed one application, that application different file for OS to OS). Same application LINUX for different file and WINDOWS for the different file similarly MAC.
* Infra tools like DOCKER, KUBERNETES, DGRAPH and  COCKROACH DB made up of Golang.
* Go supports for CONCURRENCY, which is ability to run multipule tasks simultaneously.
* CONCURRENCY is achieved in Go through the use of GOROUTINES and CHANNELS.
* &nbsp;MISSING  ->"TRY and CATCH" missing we never need in this language.

&nbsp;            -> "LEXER" done the lot of work. In the end of the every line we don't want to put the SEMICOLAN ';' but according to the golang documentation must put the                      SEMIOCOLAN but lexer done that job.



* Programs are ASSEMBLED by using the PACKAGES, for efficient management of the dependencies.
* Supports environment adopting patterns like to DYNAMIC.
* FEATURES -> 1.Garbage Collection
              2.Fast Compilation
              3.Cross Platform Support
              4.Built in TESTING Support
              5.Type safety
              6.Automatic Memory Management
              7.Concurrency
* 



DEBUGGING in VS CODE
--------------------------

🔹 Simple Summary
Action	Key
Next line	            F10
Inside function	        F11
Exit function	        Shift + F11
Start/Continue	        F5
Restart                 Shift + F12
Stop Debugging          Shift + F5

KEY WORDS in GOLANG
---------------------
1. struct   6. float     11. Continue   16. main     21. type    26. goto       31. fallthrough
2. for      7. byte      12. switch     17. select   22. func    27. map        32. default
3. if       8. rune      13. defer      18. go       23. case    28. range
4. else     9. complex   14. package    19. chan     24. const   29. return
5. int      10. break    15. import     20. uint     25. var     30. interface


SOME IMPORTANT FUNCTIONS (Built in functions)
----------------------------------------------
1. make     2. new      3. len      4. cap      5.  append      6.  copy
7. close    8. delete   9. complex  10.real     11. imag        12. panic
13. recover

Operators using in this language
-----------------------------------


